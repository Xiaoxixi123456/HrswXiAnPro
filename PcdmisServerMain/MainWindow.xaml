<Window x:Class="PcdmisServerMain.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PcdmisServerMain"
        mc:Ignorable="d"
        Title="PcdmisServer" Height="171.586" Width="355.122" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" 
              Margin="0,2,-0.2,1.6"
              HorizontalContentAlignment="Stretch" 
              VerticalContentAlignment="Stretch">
            <MenuItem Header="设置">
                <MenuItem Header="程序设置"/>
                <MenuItem Header="目录设置"/>
                <MenuItem Header="报告目录"/>
            </MenuItem>
            <MenuItem Header="日志">
                <MenuItem Header="日志记录"/>
            </MenuItem>
        </Menu>
        <Button Content="最小化" Grid.Row="1" 
                Margin="20, 10"/>
        <StatusBar Grid.Row="3">
            <Label Content="{Binding StatusInfo}"/>
        </StatusBar>
    </Grid>
</Window>
