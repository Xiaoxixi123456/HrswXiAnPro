<UserControl x:Class="WPFBindingTest.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFBindingTest"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Person}">
    <StackPanel>
        <TextBox x:Name="textBlock" TextWrapping="Wrap" Text="{Binding Name}"/>
        <TextBlock x:Name="textBlock1" TextWrapping="Wrap" Text="{Binding Age}"/>
    </StackPanel>
</UserControl>
