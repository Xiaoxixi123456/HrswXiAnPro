<UserControl x:Class="MainApp.Views.LogicUnitUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500"
             DataContext="{Binding LogicUnits[0].LogicUnit}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="227*"/>
            <RowDefinition Height="73*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="listBox" 
                 Margin="3" 
                 Grid.Row="0"
                 Grid.Column="0" 
                 ItemsPanel="{DynamicResource ItemsPanelTemplate1}" 
                 ItemTemplate="{DynamicResource CmmPartDataTemplate}" 
                 ItemsSource="{Binding CurrentTray.Parts}"
                 />
        <StackPanel Orientation="Vertical" Margin="3" Grid.Column="0" Grid.Row="1">
            <BulletDecorator Margin="0,2" >
                <BulletDecorator.Bullet>
                    <Label VerticalContentAlignment="Center" Content="CMM:"/>
                </BulletDecorator.Bullet>
                <Label VerticalContentAlignment="Center" Content="{Binding CmmName}" />
            </BulletDecorator>
            <BulletDecorator Margin="0,2" >
                <BulletDecorator.Bullet>
                    <Label VerticalContentAlignment="Center" Content="CMM:"/>
                </BulletDecorator.Bullet>
                <Label VerticalContentAlignment="Center" Content="{Binding CurrentTray.Id}" />
            </BulletDecorator>
        </StackPanel>

    </Grid>
</UserControl>
